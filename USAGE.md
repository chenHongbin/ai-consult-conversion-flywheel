# AI咨询转化飞轮使用说明书

版本：v1.9

一句话理解：**安装后先用基础咨询运行时解决当天的问题；再让 AI 认识机构、蒸馏销冠完整的销售逻辑和流程，最后每天用它分析咨询、训练新人和管理团队。**

## 3.5 安装后无需蒸馏即可使用

安装包自带基础咨询运行时，不需要先上传知识库或录音。直接说：

```text
分析这段微信，找出患者为什么没有继续回复。
患者说费用太贵，帮我判断真实顾虑并写下一句。
陪我练一次“担心过去治疗失败”的患者。
帮我设计一次爽约后的回访。
```

总入口会自动选择专项能力。没有机构蒸馏时，输出的是通用咨询方法，不会编造本机构的价格、医生、地址、项目、疗效或治疗周期。需要机构专属回答时，再执行初始化和蒸馏。

后台可以检查路由：

```bash
python3 scripts/route_consultation.py "患者说费用太贵，担心乱收费"
python3 scripts/route_consultation.py --list
```

路由结果会显示专项能力、当前运行模式、匹配依据、需要的输入和输出结构；用户正常使用时不需要看到这些技术字段。

## 3.6 本地初始化必须使用标准目录

如果选择本地工作区，Skill 只允许创建：

```text
用户选择的文件夹/
└── 咨询转化工作区/
    ├── 01_机构介绍/
    ├── 02_科室项目与服务/
    ├── 03_咨询流程与标准/
    ├── 04_优秀咨询案例/
    ├── 05_普通失败与未预约案例/
    ├── 06_团队培训与反馈/
    ├── 07_我的产出/
    ├── 08_团队管理/
    ├── _系统/
    └── README_先看这里.md
```

初始化必须执行并校验：

```bash
python3 scripts/init_consult_workspace.py <工作空间根目录> --name 咨询转化工作区
python3 scripts/verify_consult_workspace.py <工作空间根目录>
```

不要手工创建“知识库”“资料库”“客户资料”等替代目录。校验返回 `status: canonical` 后，才算初始化完成。原有资料不会被移动、改名或删除；首次蒸馏仍会扫描用户授权的整个资料来源根目录。

## 1. 它解决什么问题

AI咨询转化飞轮面向民营医疗机构咨询师、咨询主管和咨询负责人，主要解决：

- 分析电话、微信、私信，找到客户流失节点；
- 从销冠录音中提炼完整销售逻辑，并用于新人陪练；
- 按机构、科室、病种和渠道调用咨询知识；
- 判断客户阶段、真实需求和潜在顾虑；
- 处理费用、效果、信任、距离、时间和家人意见；
- 管理预约、爽约、未到院、回访和团队训练。

## 2. 最重要的原则

蒸馏的主产物不是金句，也不是 FAQ，而是：

> **销冠完整的销售逻辑、判断过程和销售流程。**

完整链路包括：

```text
线索进入
→ 破冰与首响
→ 获取联系方式
→ 深挖需求
→ 判断阶段与真实顾虑
→ 解释匹配价值
→ 处理费用/效果/信任等顾虑
→ 邀约到院
→ 到院前确认
→ 到院承接
→ 未成交回访
→ 爽约、未到院和长期培育
```

客户顾虑、流失节点、客户常见 100 问、新人必知必会 200 问和 AI 陪练，都是从这条完整链路上拆出来的专项模块。

## 3. 安装

### 下载

当前本地生成的 v1.8 安装包：

`AI咨询转化飞轮_v1.8.skill`

发布到 GitHub 后，资产文件名统一为：`ai-consult-conversion-flywheel-v1.8.skill`。

在 WorkBuddy、Trae、Codex 或 Claude 等支持 Skill 的产品中安装。

安装后直接说：

```text
第一次使用，帮我建立机构和咨询转化工作区。
```

不需要先理解“知识库、蒸馏器、能力包、Schema”等技术词。

## 4. 第一次使用：两种情况

### 情况 A：工作空间里已经有其他资料

安装后不需要先整理资料。第一次说“蒸馏销冠”时，Skill 会扫描当前工作空间根目录中的全部候选资料：

```text
第一次使用 AI咨询转化飞轮。请递归扫描当前工作空间根目录，盘点已有的文档、PDF、表格、录音、微信 HTML、聊天截图和长图，不要只读取已经转写好的文本。

不要移动、改名、上传或删除原文件。输出资料数量、类型、日期范围、可能的机构资料、咨询案例、培训资料、敏感资料和无关资料，并继续处理全部候选资料。
原文件必须保持不变。
```

Skill 会在当前目录建立标准入口，原资料不移动、不删除；所有可读咨询、录音、聊天、截图 OCR、会议和内训资料都会进入处理清单，再按“患者咨询/方法参考/未知”分层，不会因为缺少“已到院/未到院”标签而跳过。

```text
咨询转化工作区/
```

原有资料保持不动。目录混杂时优先选择这个方式。

### 情况 B：没有机构资料，只有很多录音

可以直接开始，但第一阶段只能生成“临时咨询能力包”，不能生成完整的机构专属能力包。

直接说：

```text
我现在没有机构知识库，但有一批咨询录音，想先蒸馏咨询销冠。

请先盘点录音数量、格式、时长、重复文件、转写质量、说话人识别、可能的科室/病种/渠道、结果标签缺口和隐私风险。
先不要猜测价格、医生、地址、疗效、治疗周期和流程。
请先生成“临时咨询能力包 v0.1”。
```

建议数量：

- 10～20 条：检查流程和资料质量；
- 30 条以上：形成初步行为规律；
- 50～80 条：形成较稳定的首轮销冠能力包。

必须同时包含优秀和普通/失败案例，不能只提供成功录音。

## 5. 初始化机构

初始化时提供：

```text
请初始化我的 AI咨询转化飞轮。

机构名称：
当前科室：
主要病种/项目：
资料来源：本地文件夹 / IMA / 两种都用
是否允许用于团队学习：是 / 否 / 部分资料允许

请只使用已确认资料，不要编造医生、价格、地址、疗效、治疗周期或机构流程。
```

初始化只建立机构事实，不会自动把案例经验当成机构规则。

## 6. 蒸馏后如何真正更新 Skill

安装包本身不会被每家机构改写。蒸馏结果会保存在当前工作空间的：

```text
咨询转化工作区/_系统/当前能力包/
├── active.json
└── versions/
    ├── v0.1/package.json
    └── v0.1/runtime-context.md
```

首次或增量蒸馏完成后，Skill 会把候选经验整理成结构化 `candidate.json`，经过覆盖率、证据、反例、隐私和固定测试后，生成新版本并更新 `active.json`。以后分析电话、微信、陪练和团队复盘时，会优先读取当前 active 能力包，所以回答会逐渐贴近本机构的知识、销冠逻辑和管理反馈。

基础 Skill 保持不变，机构数据不进入公共安装包。需要撤回新版本时，系统只切换运行指针，不删除旧版本或原始资料。

### v1.4：机构知识同步蒸馏

每轮蒸馏会同时更新两类候选：

- 咨询能力：销冠逻辑、阶段动作、顾虑处理和陪练；
- 机构知识：机构介绍、科室项目、服务、医生、优势、资质和脱敏案例。

录音里的机构说法只能作为候选证据。价格、医生、地址、活动、资质、疗效、周期和案例结果必须经过管理者确认后才会进入运行时。冲突或待确认知识会单独保存，不会被团队 Skill 使用。

本地知识运行在：

```text
咨询转化工作区/_系统/当前机构知识/
├── active.json
└── versions/
```

### v1.5：患者决策洞察与 AI 陪练

每轮全量或增量蒸馏还会生成第三类候选：患者决策洞察。它不是患者个人画像，而是从去重、脱敏后的咨询案例中提炼出的群体级决策状态、常见疑义意图、流失节点和合成陪练场景。

第一版建议沉淀 5—8 个状态、10—15 个高频疑义和 8—12 个陪练场景。每张状态卡都必须有可观察证据、候选解释、验证问题、支持案例、反例、适用范围和审核状态。不能推断收入、职业、性格、心理、支付能力或成交潜力。

候选写入：

```bash
python3 scripts/commit_patient_insight_candidate.py <工作空间根目录> <patient-insight-candidate.json>
```

只有完成证据、反例、固定测试、隐私审核和管理者审核后，才允许加 `--publish`。团队发布包会同步已审核的患者决策状态和合成陪练索引，但不会携带原始录音、微信聊天或患者个人资料。

陪练流程固定为：患者说一句 → 学员回复 → 多轮互动 → 只指出一个关键问题 → 学员重答。前台只显示“做对的、漏掉的、下一句建议”。

### v1.6：可靠资料处理、影子试用与统一发布

v1.6 解决首次蒸馏最容易卡住的三个问题：资料很多但只处理了一条、OCR/转写失败导致整批停住、三类能力版本不同步。

- 音频、长图、微信 HTML、PDF、DOCX、XLSX、PPTX 统一进入处理清单；原图先切片，OCR 结果保留原图回链；
- 自动记录派生文本质量。低质量 OCR 仍保存，但降低证据权重；单条失败进入失败清单并继续其他资料；
- 全量处理后自动生成影子试用队列，挑选正向、反向、对照和结果未知案例，直接按咨询转化八步法生成复盘，不先阻塞在机构问卷；
- 咨询能力、机构知识、患者洞察分别审核写回后，使用一个统一 release 绑定；团队包只从该 release 构建，回滚也同步回滚三类组件。

后台命令：

```bash
python3 scripts/run_full_distillation.py <工作空间根目录> --run-transcription --run-ocr
python3 scripts/run_shadow_analysis.py <工作空间根目录> --count 3
python3 scripts/publish_release.py <工作空间根目录> --version v1.8
python3 scripts/build_team_skill_package.py <工作空间根目录> --output-dir <发布目录> --institution <机构> --department <科室>
```

如果需要恢复上一个团队运行版本：

```bash
python3 scripts/rollback_release.py <工作空间根目录> --previous
```

### v1.7：咨询视觉弹药与嵌入式医疗生图

每次咨询分析不只给“怎么回”，还可以给出一套可直接执行的素材包：朋友圈短文案、微信下一步文案、推荐视觉类型和配套图片。用户仍然只安装一个 AI咨询转化飞轮 Skill，医疗生图引擎藏在 `skills/medical-image-studio/` 后面。

支持的咨询素材包括：

- 匿名案例示意、过程示意和患者教育图；
- 到院流程、检查准备、费用/服务说明卡；
- 医院环境示意、医生接诊示意和医生科普封面；
- 朋友圈海报、微信跟进卡和团队培训对比卡。

三条可直接试跑的提示词见 [consultation-visual-content-loop.md](references/consultation-visual-content-loop.md)。首次配置只在本地终端执行：

```bash
node <Skill目录>/skills/medical-image-studio/scripts/medical-image.mjs --doctor
node <Skill目录>/skills/medical-image-studio/scripts/configure.mjs
```

当前嵌入引擎的服务商适配器以文生图为主；图生图要等服务商参考图接口适配完成后启用，不能把文字描述当成已经上传了真实图片。

### v1.8：IMA 配额感知与团队/个人双层成长

v1.8 增加两个后台闭环：

- `scripts/ima_sync.py`：维护 IMA 资料清单、优先级、检索队列、配额事件和受控缓存索引。首次批量蒸馏默认按“正向 50%、反向 30%、其他 20%”排序；遇到 IMA 限额立即暂停并断点续传。
- `scripts/personal_growth.py`：维护一线咨询师自己的案例、个人能力候选、训练反馈和团队版本绑定。团队版本更新时，个人数据保留；依赖旧团队规则的个人候选会标记为待重新验证。

IMA 清单和队列示例：

```bash
python3 scripts/ima_sync.py inventory <工作空间根目录> \
  --input <IMA列表响应.json> --knowledge-base <知识库名称>
python3 scripts/ima_sync.py queue <工作空间根目录> --limit 20
python3 scripts/ima_sync.py record <工作空间根目录> --media-id <媒体ID> \
  --quota-error --error "资料获取次数已达上限，请明天再尝试"
```

个人成长示例：

```bash
python3 scripts/personal_growth.py init <工作空间根目录> --operator A001_张三 --team-release Team-v1.8
python3 scripts/personal_growth.py case <工作空间根目录> --source-id case-001 \
  --role positive --summary "费用顾虑先共情后追问" --outcome "预约"
python3 scripts/personal_growth.py rebase <工作空间根目录> --team-release Team-v1.9
python3 scripts/personal_growth.py compose <工作空间根目录>
```

团队成员仍然只安装一个 `.skill`。团队包包含一线所需的转录、OCR、扫描、分析、陪练和个人成长能力，但不包含主管的全团队蒸馏、候选写回、发布、回滚和团队报告脚本。

### 支持的资料源

- 本地工作空间；
- IMA 知识库；
- WorkBuddy、Trae、Codex 或 Claude 可访问的普通文件夹。

在 IMA 中，先列出知识库，再由用户指定。不要根据历史示例自动选择知识库。

## 7. 标准蒸馏流程

### 第一步：资料盘点

首次运行扫描整个当前工作空间，之后根据文件哈希只处理新增或发生变化的资料。保留原文件路径和哈希，识别录音、微信 HTML、聊天截图、长图、教学示例和无关资料。

后台可用脚本：

```bash
python3 scripts/inventory_workspace.py <工作空间根目录>
python3 scripts/run_full_distillation.py <工作空间根目录> --run-transcription --run-ocr
```

覆盖率未完成时只能生成“部分样本候选分析”，不能命名为完整销冠能力包。处理完成后会自动生成影子试用队列；影子试用是验证报告能否跑通，不会修改 active 版本。

### 第二步：转写与 OCR

- 录音默认使用 YouNavi 转写；
- 长图先切片，再 OCR；
- 微信 HTML 保留消息顺序；
- 保留说话人、时间点和原始证据位置；
- 姓名、电话、微信号、病历号和头像必须脱敏。

### 第三步：案例标签

每条案例尽量标记：

```text
机构、科室、病种/项目、渠道、咨询师、咨询阶段、客户顾虑、是否留资、是否预约、是否到院、是否成交、是否回访、结果来源、授权状态
```

### 第四步：同步提取机构知识

每次处理录音、微信和机构资料时，同时整理：机构介绍、科室项目与服务、医生与团队、科室优势与资质、脱敏案例和咨询表达资产。

输出机构知识候选时，每条内容必须带来源文件、录音时间点或聊天位置、来源类型、审核状态和生效/失效时间。然后运行：

```bash
python3 scripts/commit_knowledge_candidate.py <工作空间根目录> <knowledge-candidate.json> --publish
```

只有 `approved`、`confirmed` 或 `active` 状态的知识会进入运行时。

### 第六步：蒸馏销冠完整销售流程

使用：

```text
请不要只提炼销冠金句。请蒸馏这批案例中优秀咨询师完整的销售逻辑和销售流程。
请逐段分析：线索进入、破冰首响、获取联系方式、深挖需求、阶段判断、真实顾虑、价值解释、顾虑处理、邀约到院、到院前确认、到院承接、未成交回访、爽约与长期培育。

每个阶段输出：客户信号、销冠观察、销冠判断、关键动作、推进时机、下一阶段进入条件、普通/失败案例断点、机构事实和不可外推内容。
最终输出“销冠完整销售逻辑图”和“阶段动作卡”，不要生成脱离上下文的金句合集。
```

### 第七步：提炼专项顾虑

优先分析：

1. 费用贵、担心乱收费、不知道值不值；
2. 担心效果、过去治疗失败；
3. 不信任医院、医生或案例；
4. 距离、时间、家人意见；
5. 预约犹豫、爽约、未到院和回访失联。

专项提示词：

```text
在完成销冠全链路蒸馏后，请沿着每个阶段提取客户顾虑和流失节点。
请说明顾虑出现在哪个阶段、客户真正担心什么、销冠如何判断、如何回应、回应后是否进入下一阶段，以及普通/失败案例的不同。
不要只统计出现次数，也不要只摘录话术。
```

### 第八步：固定测试与影子运行

新能力必须用未参与蒸馏的案例测试，检查：

- 阶段判断；
- 真实顾虑识别；
- 销冠动作识别；
- 下一步建议；
- 医疗安全；
- 是否过度外推。

首轮只影子运行，不直接用于绩效定级、不自动发布、不替代医生判断。

## 7. 蒸馏后的主要产物

初始化后会自动建立：

```text
06_团队培训与反馈/
├── 01_新人培训/
└── 02_训练记录与复盘/

07_我的产出/
├── 01_流失节点报告/
├── 02_客户顾虑与标准回应/
├── 03_销冠蒸馏能力包/
├── 04_咨询分析与陪练/
└── 05_患者决策洞察与陪练/
```

### 客户常见 100 问

不是把一句话改写成 100 种，而是提炼 100 个客户问题意图。每张卡应包含：

- 客户问题与常见表达；
- 所属阶段和真实顾虑；
- 销冠回应逻辑；
- 已审核证据；
- 标准回应结构；
- 适用条件和反例；
- 下一步动作；
- 禁用表达和转人工边界。

### 新人必知必会 200 问

这是训练题库，不是一次性背诵的固定话术。每题包含场景、判断点、合格回应结构、常见错误、评分标准和陪练变化场景。

训练节奏建议：

```text
知道 → 模仿 → 变式 → AI陪练 → 真实录音复盘 → 一周后复测
```

## 8. 日常使用

### 分析单条咨询

```text
分析这条咨询，告诉我：客户当前阶段、真实顾虑、流失风险、销冠可能怎么处理，以及下一步最应该做什么。
机构：
科室/病种：
渠道：
```

### 处理客户顾虑

```text
客户说：“太贵了。”
请先判断他是在表达价格问题、价值问题还是信任问题，再给出符合当前阶段的回应结构，不要直接降价或承诺疗效。
```

### 新人陪练

```text
请根据本机构销冠能力包，给新人练一个“担心效果、过去治疗失败”的场景。
一次只训练一个动作，先让新人回答，再指出一个最关键的改进点并让他重答。
```

### 生成团队报告

```text
请生成本周团队报告。
重点查看每名成员在客户阶段判断、费用顾虑、效果顾虑、信任顾虑、预约和回访中的一个主要短板。
每人只给一个本周优先训练动作，并附上证据和复查样本。
```

## 9. 团队管理与夜间任务

员工资料放入：

```text
08_团队管理/01_成员/员工编号_姓名/01_今天放这里/
```

会议录音放入：

```text
08_团队管理/02_团队会议/01_今天放这里/
```

团队数据放入：

```text
08_团队管理/03_团队数据/01_今天放这里/
```

每天 22:00 由 WorkBuddy、Codex 等 Agent 运行夜间任务：

```text
扫描新资料
→ 后台归档
→ 哈希去重
→ 音频转写/OCR/聊天解析
→ 生成分析队列
→ 输出个人和团队报告
```

原文件保留不动；后台归档、队列和日志放在 `_系统/`。

## 10. 安全边界

- 不生成虚假的患者案例、治疗前后效果、医生、医院环境和资质；
- 不猜测价格、地址、医生信息、疗效和治疗周期；
- 不把单个成功案例直接变成标准流程；
- 不把客户顾虑回应变成诊断、用药或治疗建议；
- 医疗问题转人工或医生；
- 不自动发送微信、不自动绩效定级、不自动发布能力包；
- IMA 知识库和本地机构资料不会写进公开 Skill；
- 患者姓名、电话、微信、病历号和头像不得进入通用知识库。

## 11. 常见问题

### 没有知识库能不能用？

可以。先生成临时咨询能力包，学习销冠行为和流程；补齐机构知识后，再升级为机构专属能力包。

### 只有销冠录音可以吗？

可以开始，但最好补充普通、失败、未预约和未到院案例，否则容易把个人风格误当成通用规律。

### 为什么不能直接生成标准话术？

因为标准回应必须绑定客户阶段、顾虑、病种、渠道和机构事实。脱离条件的固定话术容易被错误套用。

### 多久蒸馏一次？

首次可集中蒸馏；后续每周、每两周或每月增量更新。每轮都要测试、审核和保留旧版本。

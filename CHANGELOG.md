# 更新记录

本项目遵循语义化版本。公共 Core 版本与机构工作区内的运行时版本相互独立。

## 2.2.1 - 2026-08-20

### 修复

- 首次设置固定为三阶段和不超过五轮的一次一问采访，角色始终是第一问；
- 新增可恢复采访状态机，禁止 Agent 自行合并、改序或跳过问题；
- 用户已新建本地文件夹时，确认后只在其下创建标准 `咨询转化工作区/`；
- 团队人数不再被当作成员名单，没有真实工作区校验证据时不能宣称创建成功；
- 清理旧版“先选本地/IMA”的初始化指令，资料接入移到首次设置完成后；
- 新增 YouNavi 宿主标识与旧工作区缺失信息续问路径。
- 新增零工作空间启动：临时采访状态、默认安全目录建议、确认后自动创建和自选位置；
- 新增无本地文件能力的临时单条分析模式，并明确关闭持久化、团队复盘和自动运行。

## 2.2.0 - 2026-08-20

### 新增

- 建立 Public Core、机构工作区、机构运行时和个人成长层四层契约；
- 根目录 `VERSION` 成为公共 Core 版本唯一真源；
- 新增环境与升级隔离检查 `scripts/doctor.py`；
- 新增公共安装包文件哈希和独立产物复核；
- 新增 GitHub CI、Tag Release、贡献指南和安全报告流程。
- 建立单一 `.skill` 交付模式，GitHub 与国内渠道不再产生不同交付包；
- 新增自然语言产品反馈入口和本地脱敏反馈卡，不默认上传数据。

### 调整

- 普通用户始终安装同一个公共 Core，机构能力从本地工作区加载；
- 原“团队 Skill 包”改为可选离线团队运行包，不再是默认产品路径；
- `SKILL.md` 改为短主路由，详细规则按当前任务渐进加载；
- 工作区必须创建在 Skill 安装目录之外，避免 Core 更新覆盖用户数据。

### 保留

- V2.1.3 的隐私白名单、独立审核、统一发布、运行时哈希、每日任务租约、长截图 OCR 和安全模式。

## 2.1.3 - 2026-08-20

- 可信运行与发布加固：公共包白名单、审核回执、运行时完整性、内容资产状态机、每日复盘恢复和长截图真实链路。

更早版本的详细记录保存在 `references/v*-release-notes.md`。
